model:
  n_width: 26
  n_depth: 1
  learning_rate: 0.001
  epoch_max: 30
  seed: 42

data:
  table : "ecml.data.tutorial_data"
  batch_size: 32
  version: -1

logger:
  verbosity: 1
  experiment_root : "ecml"

hpo: 
  trials: 30
  warmup_trials: 5
  warmup_steps: 2

retrain_threshold: 1.25

